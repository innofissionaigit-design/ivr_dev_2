# Vast.ai instance this tree is pushed to.
# Lives OUTSIDE merged_code/ on purpose: vast_push.py uploads every file under
# the repo root, so a token kept in there would be shipped to the pod (and into
# any future git checkout). Update these two lines when you rent a new instance
# -- the portal URL is https://<BASE>/terminals/1?token=<TOKEN>.
VAST_BASE=https://151.237.25.16:25744
VAST_TOKEN=762238c4fbb7265509bacb9996b1c2da727c0637dd2c49ef51bbeb44ed8c93fb
VAST_DEST=workspace/kolkata-care-voice-agent
